# 贷款体检器后端产品池表结构设计

**版本**：v0.1  
**创建日期**：2025-12-02  
**业务定位**：企业资金体检 + 贷款体质手术平台

---

## 📊 核心业务逻辑

### 前端：贷款体检器
```
企业老板输入基本信息 → 系统诊断"体质" → 生成体检报告 → 推荐"手术方案"
```

### 后端：产品池
```
产品池（贷款产品 + 资金方 + 贸易通道） ← 智能匹配引擎 ← 企业体质数据
```

### 8个业务模块的角色
```
A/B/C/D/E/F/G/H业务 = 手术工具包
- 不是独立业务线
- 是帮企业"改善体质"的工具模块
- 根据体检结果，组合使用不同工具
```

---

## 🗄️ 数据库表结构设计

### 表1：product_pool（产品池主表）

**用途**：存储所有贷款产品、资金方、贸易通道的基础信息

```sql
CREATE TABLE product_pool (
    -- ========== 基本信息 ==========
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '产品ID',
    product_code VARCHAR(50) UNIQUE NOT NULL COMMENT '产品编码（如：LOAN001, TRADE001）',
    product_name VARCHAR(200) NOT NULL COMMENT '产品名称',
    partner_name VARCHAR(200) COMMENT '合作方名称（资金方/贸易商）',
    partner_type ENUM('银行', '小贷', '保理', '贸易商', '其他') COMMENT '合作方类型',
    industry_category VARCHAR(100) COMMENT '行业品类（如：电子、农产品、有色金属）',
    
    -- ========== 资金/产品属性 ==========
    product_type ENUM('纯信用贷', '抵押贷', '质押贷', '担保贷', '供应链融资', '贸易融资', '其他') NOT NULL COMMENT '产品类型',
    funding_cost_annual DECIMAL(5,2) COMMENT '资金年化成本（%）（给我们的成本）',
    client_cost_annual DECIMAL(5,2) COMMENT '客户年化成本（%）（给客户的报价）',
    amount_min DECIMAL(15,2) COMMENT '最小额度（元）',
    amount_max DECIMAL(15,2) COMMENT '最大额度（元）',
    term_min INT COMMENT '最短期限（天）',
    term_max INT COMMENT '最长期限（天）',
    collateral_requirement TEXT COMMENT '抵押担保要求（JSON格式，如：{"type":"房产","ratio":"70%"}）',
    
    -- ========== 准入条件 ==========
    accept_ticket BOOLEAN DEFAULT FALSE COMMENT '是否接受票据',
    accept_tax_required BOOLEAN DEFAULT FALSE COMMENT '是否需要开票纳税',
    revenue_min DECIMAL(15,2) COMMENT '最低营业额要求（元/年）',
    credit_score_min INT COMMENT '最低征信评分要求',
    industry_whitelist TEXT COMMENT '行业白名单（JSON数组，如：["制造业","贸易"]）',
    industry_blacklist TEXT COMMENT '行业黑名单（JSON数组，如：["房地产","矿产"]）',
    region_whitelist TEXT COMMENT '地区白名单（JSON数组）',
    region_blacklist TEXT COMMENT '地区黑名单（JSON数组）',
    other_requirements TEXT COMMENT '其他准入条件（JSON格式）',
    
    -- ========== 与业务模块关系 ==========
    compatible_modules TEXT COMMENT '能配合的业务模块（JSON数组，如：["A","B","H"]）',
    suitable_physique_types TEXT COMMENT '适合的体质类型（JSON数组，如：["流水不足","票据资产多","征信良好"]）',
    report_recommendation_position ENUM('首推', '次选', '备选', '不推荐') DEFAULT '备选' COMMENT '在体检报告中的推荐位置',
    combination_logic TEXT COMMENT '组合使用逻辑（JSON格式，描述如何与A/B/H等模块组合）',
    
    -- ========== 商务与风控 ==========
    revenue_model ENUM('利差', '服务费', '佣金', '组合') COMMENT '收益模式',
    our_profit_rate DECIMAL(5,2) COMMENT '我方利润率（%）',
    cooperation_status ENUM('正常合作', '暂停合作', '考察中', '已停止') DEFAULT '正常合作' COMMENT '合作状态',
    risk_points TEXT COMMENT '风险点（JSON数组，如：["资金方风控严格","需要真实贸易背景"]）',
    compliance_boundary TEXT COMMENT '合规边界（JSON格式，描述合规要求和禁止事项）',
    approval_rate DECIMAL(5,2) COMMENT '历史通过率（%）',
    avg_approval_days INT COMMENT '平均审批天数',
    
    -- ========== 系统字段 ==========
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否启用',
    priority_score INT DEFAULT 50 COMMENT '优先级评分（0-100，用于智能推荐排序）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    created_by VARCHAR(100) COMMENT '创建人',
    updated_by VARCHAR(100) COMMENT '更新人',
    remarks TEXT COMMENT '备注'
);

-- 索引
CREATE INDEX idx_product_type ON product_pool(product_type);
CREATE INDEX idx_cooperation_status ON product_pool(cooperation_status);
CREATE INDEX idx_is_active ON product_pool(is_active);
CREATE INDEX idx_priority_score ON product_pool(priority_score);
```

---

### 表2：physique_types（体质类型定义表）

**用途**：定义企业的"贷款体质"类型，用于智能匹配

```sql
CREATE TABLE physique_types (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '体质类型ID',
    type_code VARCHAR(50) UNIQUE NOT NULL COMMENT '体质编码（如：POOR_CASH_FLOW）',
    type_name VARCHAR(100) NOT NULL COMMENT '体质名称（如：流水不足型）',
    description TEXT COMMENT '详细描述',
    diagnostic_criteria TEXT COMMENT '诊断标准（JSON格式，如何判断企业属于这种体质）',
    typical_problems TEXT COMMENT '典型问题（JSON数组）',
    recommended_solutions TEXT COMMENT '推荐解决方案（JSON数组，包含业务模块组合）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 示例数据
INSERT INTO physique_types (type_code, type_name, description, diagnostic_criteria, typical_problems, recommended_solutions) VALUES
('POOR_CASH_FLOW', '流水不足型', '银行流水少，贷款难度大', '{"revenue_annual":"<500万","bank_flow_ratio":"<30%"}', '["流水不足","银行不认可","贷款额度低"]', '[{"modules":["H","B"],"desc":"先做H业务养流水，再用B业务优化付款"}]'),
('TICKET_ASSET_RICH', '票据资产多', '手上有大量票据/凭证，但缺现金', '{"ticket_amount":">100万","cash_ratio":"<20%"}', '["票据多现金少","资金周转慢"]', '[{"modules":["A","B"],"desc":"A业务变现票据，B业务用票据付款"}]'),
('GOOD_CREDIT', '征信良好型', '征信好，资质优，但额度需求大', '{"credit_score":">700","revenue_annual":">1000万"}', '["额度需求大","银行额度不够"]', '[{"modules":["纯信用贷","H"],"desc":"纯信用贷为主，H业务做大流水提升额度"}]'),
('COLLATERAL_WEAK', '抵押物不足', '有资金需求，但缺乏抵押物', '{"collateral_value":"<贷款需求50%"}', '["无抵押物","质押物不足"]', '[{"modules":["H","供应链融资"],"desc":"H业务做大应收应付，申请供应链融资"}]'),
('REGISTRATION_ISSUE', '注册资本未实缴', '注册资本认缴但未实缴，影响授信', '{"registered_capital_paid_ratio":"<50%"}', '["未实缴影响授信","银行额度受限"]', '[{"modules":["G"],"desc":"G业务专利实缴，提升资质"}]'),
('BID_NEED', '招投标需求型', '需要参加招投标，需要资金证明', '{"bid_requirement":"资金证明"}', '["需要资金证明","账户余额不足"]', '[{"modules":["F","H"],"desc":"F业务摆账，H业务做大流水"}]');
```

---

### 表3：client_physique_diagnosis（客户体质诊断记录表）

**用途**：记录每个客户的体检结果和诊断数据

```sql
CREATE TABLE client_physique_diagnosis (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '诊断ID',
    client_id INT NOT NULL COMMENT '客户ID（关联客户表）',
    diagnosis_date DATE NOT NULL COMMENT '诊断日期',
    
    -- ========== 企业基础数据 ==========
    company_name VARCHAR(200) COMMENT '企业名称',
    industry VARCHAR(100) COMMENT '所属行业',
    registered_capital DECIMAL(15,2) COMMENT '注册资本（元）',
    paid_capital DECIMAL(15,2) COMMENT '实缴资本（元）',
    revenue_annual DECIMAL(15,2) COMMENT '年营业额（元）',
    bank_flow_monthly DECIMAL(15,2) COMMENT '月均银行流水（元）',
    credit_score INT COMMENT '征信评分',
    
    -- ========== 资产状况 ==========
    ticket_amount DECIMAL(15,2) COMMENT '票据资产（元）',
    collateral_value DECIMAL(15,2) COMMENT '可抵押物价值（元）',
    accounts_receivable DECIMAL(15,2) COMMENT '应收账款（元）',
    accounts_payable DECIMAL(15,2) COMMENT '应付账款（元）',
    
    -- ========== 融资需求 ==========
    loan_demand_amount DECIMAL(15,2) COMMENT '贷款需求金额（元）',
    loan_demand_term INT COMMENT '贷款期限需求（天）',
    loan_purpose VARCHAR(500) COMMENT '贷款用途',
    special_needs TEXT COMMENT '特殊需求（JSON数组，如：["招投标","快速放款"]）',
    
    -- ========== 诊断结果 ==========
    physique_types TEXT COMMENT '诊断体质类型（JSON数组，可能有多个）',
    health_score INT COMMENT '贷款健康度评分（0-100）',
    main_problems TEXT COMMENT '主要问题（JSON数组）',
    recommended_products TEXT COMMENT '推荐产品（JSON数组，包含product_id和推荐理由）',
    recommended_modules TEXT COMMENT '推荐业务模块组合（JSON数组）',
    treatment_plan TEXT COMMENT '手术方案（JSON格式，详细执行计划）',
    
    -- ========== 系统字段 ==========
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    diagnosed_by VARCHAR(100) COMMENT '诊断人/系统'
);

CREATE INDEX idx_client_id ON client_physique_diagnosis(client_id);
CREATE INDEX idx_diagnosis_date ON client_physique_diagnosis(diagnosis_date);
CREATE INDEX idx_health_score ON client_physique_diagnosis(health_score);
```

---

### 表4：module_toolkit（业务模块工具包表）

**用途**：定义8个业务模块的详细信息，作为"手术工具"

```sql
CREATE TABLE module_toolkit (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '模块ID',
    module_code CHAR(1) UNIQUE NOT NULL COMMENT '模块编码（A/B/C/D/E/F/G/H）',
    module_name VARCHAR(100) NOT NULL COMMENT '模块名称',
    description TEXT COMMENT '模块描述',
    
    -- ========== 功能定义 ==========
    primary_function VARCHAR(200) COMMENT '主要功能',
    target_problems TEXT COMMENT '解决的问题（JSON数组）',
    applicable_scenarios TEXT COMMENT '适用场景（JSON数组）',
    
    -- ========== 与产品池关系 ==========
    compatible_product_types TEXT COMMENT '配合的产品类型（JSON数组）',
    enhancement_effect TEXT COMMENT '增强效果（JSON格式，描述如何提升贷款通过率/额度）',
    
    -- ========== 执行参数 ==========
    typical_duration INT COMMENT '典型执行周期（天）',
    cost_range VARCHAR(100) COMMENT '成本区间（描述）',
    required_resources TEXT COMMENT '所需资源（JSON数组）',
    execution_partner VARCHAR(200) COMMENT '执行合作方',
    
    -- ========== 合规与风险 ==========
    compliance_requirements TEXT COMMENT '合规要求（JSON数组）',
    risk_warnings TEXT COMMENT '风险提示（JSON数组）',
    
    -- ========== 系统字段 ==========
    is_active BOOLEAN DEFAULT TRUE,
    priority INT DEFAULT 50 COMMENT '优先级（用于推荐排序）',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- 示例数据（8个业务模块）
INSERT INTO module_toolkit (module_code, module_name, description, primary_function, target_problems, applicable_scenarios) VALUES
('A', '票据/凭证贴现', '票据变现服务', '将票据/凭证快速变现', '["票据多现金少","资金周转慢"]', '["应急资金需求","日常周转"]'),
('B', '承兑/凭证销售', '票据采购服务', '用票据代替现金付款', '["现金流紧张","付款压力大"]', '["供应商付款","采购资金优化"]'),
('C', '冲存款', '存款服务', '帮助银行完成存款任务', '["闲置资金收益低"]', '["短期理财","月末季末"]'),
('D', '贸易套利指导', '套利方案设计', '真实贸易基础上的票据套利', '["利润薄","想增厚收益"]', '["有真实发票","有资金和渠道"]'),
('E', '债券投资服务', '债券配置服务', '闲置资金稳健投资', '["闲置资金收益低","税务优化需求"]', '["3-12个月闲置资金","税负优化"]'),
('F', '摆账（亮资）', '资金显账服务', '提供资金证明', '["招投标需要资金证明"]', '["招投标","银行授信"]'),
('G', '专利实缴', '注册资本实缴服务', '用专利完成实缴', '["注册资本未实缴","诉讼风险"]', '["实缴需求","资产隔离"]'),
('H', '贸易带量', '流水养护服务', '通过真实贸易做大流水', '["流水不足","贷款额度低"]', '["贷款前养流水","提升授信额度"]');
```

---

### 表5：product_module_combination（产品+模块组合方案表）

**用途**：定义产品和业务模块的最佳组合方案

```sql
CREATE TABLE product_module_combination (
    id INT PRIMARY KEY AUTO_INCREMENT COMMENT '组合方案ID',
    combination_name VARCHAR(200) NOT NULL COMMENT '方案名称',
    product_id INT COMMENT '主推产品ID（关联product_pool）',
    module_codes TEXT NOT NULL COMMENT '业务模块组合（JSON数组，如：["H","B","A"]）',
    
    -- ========== 方案定义 ==========
    target_physique_types TEXT COMMENT '针对的体质类型（JSON数组）',
    execution_sequence TEXT COMMENT '执行顺序（JSON数组，描述先做什么后做什么）',
    expected_improvement TEXT COMMENT '预期改善效果（JSON格式）',
    typical_case TEXT COMMENT '典型案例（描述）',
    
    -- ========== 执行参数 ==========
    total_duration INT COMMENT '总执行周期（天）',
    total_cost_estimate VARCHAR(100) COMMENT '总成本估算',
    success_rate DECIMAL(5,2) COMMENT '历史成功率（%）',
    
    -- ========== 系统字段 ==========
    is_recommended BOOLEAN DEFAULT TRUE COMMENT '是否推荐',
    priority_score INT DEFAULT 50,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    created_by VARCHAR(100)
);

-- 示例数据
INSERT INTO product_module_combination (combination_name, product_id, module_codes, target_physique_types, execution_sequence, expected_improvement) VALUES
('流水不足型标准方案', NULL, '["H","B","纯信用贷"]', '["POOR_CASH_FLOW"]', '[{"step":1,"module":"H","duration":60,"desc":"先做H业务养流水2个月"},{"step":2,"module":"B","duration":30,"desc":"用B业务优化付款结构"},{"step":3,"product":"纯信用贷","desc":"流水达标后申请纯信用贷"}]', '{"flow_increase":"200%","loan_amount_increase":"100万-300万"}'),
('票据资产变现+贷款组合', NULL, '["A","B","供应链融资"]', '["TICKET_ASSET_RICH"]', '[{"step":1,"module":"A","desc":"先用A业务贴现票据获得现金"},{"step":2,"module":"B","desc":"用B业务优化日常付款"},{"step":3,"product":"供应链融资","desc":"基于应收应付申请供应链融资"}]', '{"cash_flow":"立即改善","financing_amount":"票据额的70%-80%"}'),
('招投标专项方案', NULL, '["H","F","供应链融资"]', '["BID_NEED"]', '[{"step":1,"module":"H","duration":30,"desc":"做H业务快速做大流水"},{"step":2,"module":"F","desc":"招投标时用F业务提供资金证明"},{"step":3,"product":"供应链融资","desc":"中标后申请供应链融资执行项目"}]', '{"bid_success_rate":"+30%","financing_amount":"中标额的60%-70%"}');
```

---

## 🎯 智能推荐引擎逻辑

### 推荐算法流程

```
1. 企业填写基础信息 → 生成体检数据
2. 系统诊断体质类型 → 匹配physique_types
3. 根据体质类型 → 筛选compatible产品和模块
4. 计算匹配度评分 → 综合priority_score等因素
5. 生成推荐方案 → 产品+模块组合
6. 输出体检报告 → 可视化展示
```

### 匹配度评分算法（伪代码）

```python
def calculate_match_score(product, client_diagnosis):
    score = product.priority_score  # 基础评分
    
    # 额度匹配度（+20分）
    if client_diagnosis.loan_demand_amount >= product.amount_min and \
       client_diagnosis.loan_demand_amount <= product.amount_max:
        score += 20
    
    # 体质匹配度（+30分）
    if any(pt in product.suitable_physique_types for pt in client_diagnosis.physique_types):
        score += 30
    
    # 行业匹配度（+15分）
    if client_diagnosis.industry in product.industry_whitelist:
        score += 15
    elif client_diagnosis.industry in product.industry_blacklist:
        score -= 50  # 黑名单直接降分
    
    # 准入条件匹配度（+20分）
    if client_diagnosis.revenue_annual >= product.revenue_min and \
       client_diagnosis.credit_score >= product.credit_score_min:
        score += 20
    
    # 历史通过率加权（+15分）
    score += product.approval_rate * 0.15
    
    return max(0, min(100, score))  # 限制在0-100
```

---

## 📱 前端交互设计建议

### 体检器界面流程

```
Step 1: 基础信息填写
  ├ 企业名称
  ├ 所属行业
  ├ 年营业额
  ├ 月均流水
  └ 贷款需求金额

Step 2: 深度诊断问卷
  ├ 有票据资产吗？多少？
  ├ 注册资本实缴了吗？
  ├ 有抵押物吗？价值多少？
  ├ 征信评分大概多少？
  └ 有特殊需求吗？（招投标/快速放款等）

Step 3: 生成体检报告
  ├ 贷款健康度评分（0-100）
  ├ 诊断体质类型（如：流水不足型）
  ├ 主要问题列表
  ├ 推荐产品（前3名）
  └ 推荐"手术方案"（业务模块组合）

Step 4: 方案详情查看
  ├ 每个方案的详细执行步骤
  ├ 预期效果
  ├ 成本估算
  ├ 执行周期
  └ 典型案例

Step 5: 预约咨询/提交申请
  ├ 在线预约
  ├ 留下联系方式
  └ 等待专业顾问联系
```

---

## 🔧 技术实现建议

### 后端API接口设计

```
POST /api/diagnosis/submit
  - 提交体检数据
  - 返回诊断ID

GET /api/diagnosis/{id}/report
  - 获取体检报告
  - 返回推荐产品和方案

GET /api/products/search
  - 搜索产品（支持过滤条件）

GET /api/products/{id}/detail
  - 获取产品详情

POST /api/consultation/book
  - 预约咨询
```

### 数据同步策略

```
产品池数据更新：
  ├ 定期更新（每日）
  ├ 人工审核后生效
  └ 历史版本保留

客户诊断数据：
  ├ 实时生成
  ├ 加密存储（涉及企业隐私）
  └ 定期归档
```

---

## 📊 报表与分析需求

### 运营分析报表

```
1. 产品效果分析
   - 各产品推荐次数
   - 转化率
   - 通过率

2. 体质类型分布
   - 各体质类型占比
   - 主要问题分布

3. 业务模块使用情况
   - 各模块推荐频次
   - 组合方案效果

4. 客户画像分析
   - 行业分布
   - 额度分布
   - 地域分布
```

---

## ✅ 下一步工作建议

### Phase 1: MVP开发（2周）
1. 完成核心表结构创建
2. 录入20-30个样本产品数据
3. 开发基础诊断算法
4. 实现简单的推荐逻辑

### Phase 2: 数据完善（1周）
1. 补充所有8个业务模块详细信息
2. 设计10-20个组合方案
3. 完善体质类型定义
4. 录入50个以上产品数据

### Phase 3: 智能优化（1周）
1. 优化匹配算法
2. 增加机器学习预测
3. 完善推荐理由生成
4. 开发A/B测试功能

### Phase 4: 前端集成（1周）
1. 设计可视化报告模板
2. 开发在线体检器界面
3. 集成支付/预约功能
4. 上线测试

---

## 📝 附录：字段说明补充

### JSON字段格式示例

**collateral_requirement（抵押担保要求）**：
```json
{
  "type": "房产",
  "ratio": "70%",
  "valuation_method": "市场评估价",
  "requirements": ["产权清晰", "可过户"]
}
```

**compatible_modules（能配合的业务模块）**：
```json
["A", "B", "H"]
```

**suitable_physique_types（适合的体质类型）**：
```json
["POOR_CASH_FLOW", "TICKET_ASSET_RICH"]
```

**combination_logic（组合使用逻辑）**：
```json
{
  "scenario": "流水不足需要贷款",
  "steps": [
    {"order": 1, "module": "H", "duration": "60天", "description": "先做H业务养流水"},
    {"order": 2, "action": "申请本产品", "description": "流水达标后申请贷款"}
  ]
}
```

**risk_points（风险点）**：
```json
[
  "资金方风控严格，通过率约60%",
  "需要真实贸易背景",
  "征信要求较高"
]
```

**compliance_boundary（合规边界）**：
```json
{
  "allowed": ["真实贸易背景", "合法经营"],
  "forbidden": ["虚假贸易", "资金池", "违规担保"],
  "requirements": ["实名认证", "合同签署", "留痕管理"]
}
```

---

**制定人**：Claude AI + 常松阳  
**版本**：v0.1  
**状态**：设计稿，待评审
